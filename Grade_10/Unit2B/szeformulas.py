#creating function to calculate area of rhombus
def arhombus(height,base):
    area = heigth * base * 0.5
    return area

#creating function to calculate area of square
def asquare(side_length):
    area = side_length * side_length
    return area

#the perimeter of a rhombus and square are the same when the side length
#of one side is the same
#creating function to calculate perimeter of the shape
def perimeter(side_length):
    perimeter = side_length * 4
    return perimeter
